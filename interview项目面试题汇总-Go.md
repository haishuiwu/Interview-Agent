# 师练 AI（教师教学能力训练系统）项目面试题汇总

> 项目口径：这是实习期间参与的教育业务项目，面向教师资格结构化问答、教师招聘试讲答辩和青年教师发展。系统不替学校做录用决策，而是把目标标准、教学档案、训练任务、形成性评价和复测计划串成闭环。回答时可以讲教师招聘是入口，但项目立意必须落在教师教学能力发展上。

> 术语说明：当前代码为兼容历史客户端，仍保留 `JDAnalysis`、`Resume`、`InterviewState` 等类型名。面试表达统一使用“目标教师岗位/考核标准、教学档案、训练会话、学员”；必要时再解释内部字段是兼容层，不要反过来用旧字段定义产品。

## **零、项目立意与教育业务价值**

### 0.1 为什么要做教师教学能力训练，而不是继续做一个通用AI面试助手？

**分析**

这是最重要的立意题。不要从“原项目有什么技术”倒推场景，而要从教育业务中真实存在的教师成长问题讲起。

**回答**

教育产品不能只关注学生“怎么学”，教学质量还取决于教师能否基于课程标准分析学情、设计活动、组织课堂并使用评价证据调整教学。教师资格准备、试讲答辩和青年教师培养中都有大量可重复训练的环节，但传统方式依赖教研员一对一陪练，反馈成本高且难以持续。因此项目选择“教学能力训练”作为核心，Agent技术只是用来完成标准解析、个性化任务、启发式追问、形成性评价和复测闭环。

通用面试助手关注“能不能通过选拔”，这个项目关注“教学能力哪里已有证据、哪里需要练、下一轮是否改善”。所以招聘或资格面试只是一个边界清晰的训练入口，不能把项目讲成教育公司跨界做校招工具。

***

### 0.2 为什么从教师资格和教师招聘试讲切入？这不还是招聘场景吗？

**分析**

关键是区分“切入口”和“最终立意”。切入口需要标准清晰、需求高频、结果可观察，但产品价值要能延伸到教师发展。

**回答**

教师资格面试和教师招聘试讲有明确的大纲、常见任务和评价维度，便于把目标标准结构化，也便于构建教育理论、教学实践和课堂情境题库，适合作为MVP入口。但系统产出不是录用概率，而是五维教学能力诊断、训练过程指标和提升计划；同一套能力模型还可以迁移到新教师入职训练、校本教研和阶段复测。

因此我会说“从标准化教师考核切入，沉淀可持续的教师成长能力”，而不是“给招聘部门做AI筛人”。

***

### 0.3 这个项目为什么适合教育公司，而不是独立的求职产品？

**分析**

需要把项目放进教育业务链条，说明它与学生端学习、教师端教学和学校端教研的关系，不要生硬贴公司标签。

**回答**

教育公司通常已经积累学生学习、课程内容和学校服务场景，学生端解决个性化学习，教师端仍需要备课、课堂实施、学习评价和专业成长支持。这个项目补的是教师发展链路：学校或教师提供岗位/考核标准和教学档案，系统生成针对性训练，训练结果再形成可追踪的能力画像与复测计划。

它与教育业务的连接点是“提高教学与教研支持效率”，而不是拿教育流量去做泛求职。未来如果接入真实学校，数据边界也应按学校/教研组/教师隔离，公共教师题库只负责冷启动。

***

### 0.4 为什么不让大模型直接给出“是否录用”或“是不是好老师”的结论？

**分析**

这是教育伦理和可信AI边界题。回答要体现你知道模型证据有限，也知道形成性评价与高风险决策的区别。

**回答**

一次问答只能提供有限的表现证据，而且开放性教学问题通常存在多种合理方案。让模型直接输出录用结论，会把题库偏差、Prompt偏差和表达能力差异放大成高风险决定，也容易给教师贴固定标签。

所以系统把匹配分定义为“教学档案证据覆盖度”，把A/B/C/D定义为“本轮训练表现”，报告明确只服务反馈和提升。完成率、平均分、追问率等过程指标由代码计算，教学诊断由模型给出证据和建议；真正的人事决策必须由学校依据试讲、背景材料和人工评价综合完成。

***

## **一、系统架构与多Agent设计**

> 考察对多Agent架构的整体理解、技术选型决策、编排设计等核心能力。

### 1.你这个项目为什么用多Agent架构，而不是用单Agent架构？

**分析**

面试官想看你对项目的全局理解和架构决策能力。现在很多大模型应用都是一个ReAct Agent挂一堆Tool就完事了，面试官想知道你为什么要拆成多个Agent协作。关键在于讲清楚单Agent在复杂流程下的瓶颈，以及多Agent各司其职带来的好处，不能空谈理论，要结合项目实际。

**回答**

最开始我也考虑过单Agent方案，就是一个ReAct Agent，给它挂上目标标准解析、教学档案诊断、训练出题、评分这些Tool，让它自己决定调用顺序。但教师训练有严格依赖：先解析教师岗位或考核标准，再依据学员教学档案建立训练起点，然后才能规划教育理论、教学实践和课堂情境任务，最后做形成性评价。单Agent自主决定步骤时容易跳过标准解析或过早生成题目；同时各阶段Prompt要求差异很大——标准解析要稳定JSON，AI教研员要做自然的苏格拉底式追问，评价Agent还要遵守五维评价和“不得输出录用结论”的边界。全部塞进一个System Prompt容易角色串扰。

所以我拆成了6个专职Agent——目标标准解析、教学档案诊断、训练规划、AI教研员、形成性评价、提升计划，再加一个教研聊天Agent和规则路由器。它们不是自主协商，而是由中心编排器按固定DAG串联，数据通过Go强类型结构体传递。这样既保证教育训练流程可审计，也能独立优化某个环节，例如调整课堂情境追问不会影响教师题库检索。内部旧类型名暂时保留是兼容策略，对外业务语义已经切到教师训练。

***

### 2.你的多Agent系统中，这些Agent是怎么编排的？这些Agent之间是怎么协作通信的的？

**分析**

上一题确认了你为什么用多Agent，这题就要深入到"怎么做的"。面试官想听的是具体的编排方式和数据流，不是泛泛而谈。重点在于你用了什么编排模式、Agent之间的数据传递方式。

**回答**

编排模式上，我用的是中心编排器模式，而不是Agent之间自主通信。所有Agent都不知道彼此的存在，它们只和编排器（Orchestrator）打交道。编排器持有所有Agent的引用，按DAG的拓扑顺序逐个调用。这和微服务里的编排（Orchestration）vs 协同（Choreography）是一个思路——编排器掌控全局流程，每个Agent只管自己的事。

通信方式上，Agent之间不存在直接通信，全部由编排器中转。例如先调用 `jdAnalyzer.Analyze` 把教师岗位要求或考核标准解析成结构化能力要求，再把结果和教学档案传给 `resumeMatcher.Match` 建立训练起点。`JDAnalysis`、`ResumeMatchResult`、`QuestionPlan` 是历史兼容类型名，实际承载的是目标标准、教学档案证据和训练计划。Agent之间传Go结构体而不是继续传JSON字符串，编译期能约束字段，下游也不必重复解析。

状态管理上，单轮训练状态集中在Session和InterviewState里，Agent本身保持无状态，便于重试和复用。跨会话的待训练能力、历史训练得分和提升计划由记忆与存储层管理。这样教育业务状态不会散落在各个Prompt Agent中，后续做复测或成长趋势时也有统一数据来源。

***

### 3.用户输入一条消息，系统怎么判断他是想开始教师训练、上传教学档案，还是进行教研问答？

**分析**

这题考的是意图识别的技术选型。这里面试官想看你有没有根据实际场景做合理的技术选型——不是所有问题都需要用大模型来解决。回答的关键是先说清楚你怎么做的，再解释为什么这么选。

**回答**

我用规则匹配，没有用LLM做意图识别。高频入口很有限：开始训练、上传教学档案、输入教师岗位/面试大纲/考核标准、查看历史训练和普通教研聊天。对这种封闭意图集，规则路由比每条消息额外调用一次LLM更低延迟、更低成本，也更容易测试。

具体实现是维护“开始训练、试讲训练、结构化问答、教学档案、考核标准”等关键词，并识别PDF、DOCX等文件输入。训练进行中时，用户输入默认作为当前任务回答，不再走普通意图判断，避免回答里出现“课堂”“训练”等词造成误触发；未命中规则则交给AI教研助手兜底。为了平滑迁移，路由仍兼容“开始面试”等旧触发词。

***

### 4.你用的是字节跳动的Eino框架，为什么不用LangChain或者Spring AI？

**分析**

框架选型是面试高频问题，面试官想看你是否真正对比过不同方案，而不是随便选的。这里要讲清楚Eino相对于其他框架的优势，尤其是在Go生态下的适配性。

**回答**

主要原因是我的项目用Go写的，框架选型首先要适配语言生态。LangChain的Go版本（langchaingo）存在但社区不够活跃，很多Python版的功能还没移植过来，文档也不全。Spring AI是Java生态的，不在考虑范围内。

Eino是字节跳动CloudWeGo团队开源的，原生Go实现，和Go的并发模型配合得很好。它提供了我项目需要的核心能力：ChatModel抽象可以对接各种大模型API，Graph/Chain编排可以做DAG流程控制，Retriever接口可以做RAG检索，还有官方的Milvus扩展包可以直接用。另外Eino的接口设计比较克制，不像LangChain那么重，我用不到的东西不会引进来。实际开发过程中，Eino的组件接口——比如retriever.Retriever、model.ChatModel——定义得很干净，扩展自己的组件也很方便，比如我自己实现的BM25Retriever就是实现了Eino的Retriever接口，可以直接和Milvus的检索器一起组合使用。

***

### 5.你的编排器把教师教学能力训练分成哪些阶段？为什么这么分？

**分析**

这题深入到编排器的细节设计，面试官想看你对面试流程的拆解是否合理，以及是否有一些非常规的设计考量。

**回答**

编排器把完整训练拆成7个节点。第一步解析目标教师岗位、教师资格面试大纲或校本考核标准，提取学科素养、教学设计、课堂管理、评价与沟通等要求；第二步分析教学档案里的事实证据，得到当前证据覆盖度、优势和待验证能力。随后读取跨会话的历史待训练能力，并按当前目标标准过滤，避免把不相关的旧弱项带进来。

第三步是两阶段训练规划：Phase1规划教育理论、教学实践和课堂情境方向，Phase2优先从教师题库检索教育理论题，实践与情境题则结合真实教学档案动态生成。第四步由AI教研员逐题训练、评分并按缺口启发式追问；随后对低分任务做即时巩固。第五步生成教育理念与师德、学科素养、教学设计、课堂管理与应变、沟通表达与反思五维报告，第六步生成可执行、可复测的教学能力提升计划。

这么分是因为每一步都有清晰的数据依赖：没有目标标准无法界定训练边界，没有教学档案就不能做个性化实践追问，没有真实作答就不能生成形成性评价。固定DAG保证了流程稳定，“历史弱项过滤”和“低分巩固”作为中间节点又能独立迭代。

***

### 6.训练过程中学员可以中途退出吗？你怎么处理这种异常中断？

**分析**

这是一个工程化细节题，考的是你对异常情况的处理能力。很多人做项目只考虑正常流程，面试官想看你有没有考虑边界情况。

**回答**

可以，学员随时可以终止训练，我按“是否已经形成有效作答证据”分情况处理。

如果在主回答阶段退出，编排器标记 `userTerminated=true` 并跳出循环。一题未答时不生成诊断，因为没有证据；已经完成部分任务时仍生成报告和提升计划，但明确标注“原计划N题、实际完成M题”，所有评价只基于已作答内容，不把缺失数据解释为能力不足。训练完成率也由代码按真实题数计算。

如果在追问阶段退出，主回答仍写入QAHistory，追问不伪造结果；待训练能力按题实时更新，所以已完成任务产生的数据不会丢失。这符合形成性评价的原则：承认样本边界，只报告已有证据。

***

### 7.你的AI教研员在提问时，怎么管理上下文和学员画像？

**分析**

这个问题考的是LLM上下文窗口管理的实践经验。保留多少历史是一个工程上的trade-off，面试官想看你的考量。

**回答**

AI教研员每次提问时会带上最近的问答，让模型知道已经训练过什么、学员刚才的教学判断是什么，但不会把全部历史都塞进去，当前只保留最近3轮原始对话。

原因一是控制Token成本和延迟；原因二是远距离原始对话对当前追问价值有限。更早的信息不会直接丢掉，而是压缩进动态教学能力画像，记录已有证据支撑的强项、待训练能力和教学思考特征。这样既能让追问承接最近回答，又不会因完整历史不断增长而拖慢响应。

这样相当于把"精确记忆"和"概括记忆"分开了：最近3轮用原始对话保持细节，更早的用画像摘要保持全局认知。

***

## **二、RAG多路召回与检索**

> 考察RAG系统的设计与实现能力，包括多路召回、融合算法、重排序、向量数据库使用等核心知识点。

### 8.你的RAG用了向量检索和BM25双路召回，为什么不只用一路？

**分析**

这是RAG模块最核心的架构决策题，面试官想看你是否理解两种检索方式各自的优劣势，以及为什么组合使用比单独使用效果好。

**回答**

向量检索和BM25解决的问题不同。向量检索基于Embedding理解语义，例如训练方向是“依据课堂反馈调整教学”，它可以召回“形成性评价”“数据驱动教学决策”等表达不同但语义相近的教师题。它的弱点是对课程标准条目、学科术语或固定考核表述的精确命中不一定稳定。

BM25基于词频和逆文档频率，对“形成性评价”“分层提问”“家校沟通”等原词匹配很强，但不理解同义表达。两路召回后按ID去重，再交给可切换的Reranker精排，能同时保留语义召回和精确术语命中。

当前教师训练主链路是“Milvus + BM25召回 → ID去重合并 → Rerank”，并没有把两路原始分数直接相加。仓库里保留了通用的RRF MultiRetriever实现，便于离线对照或后续统一抽象，但面试时要明确主链路现状，不能把“代码里存在”说成“线上一定在用”。

***

### 9.两路召回为什么不能直接把分数相加？当前主流程和RRF实现是什么关系？

**分析**

融合算法的选择是一个有深度的话题，面试官想看你是否理解RRF的原理以及它相对于其他方案的优势。这里的陷阱是"加权求和听起来也合理"，你要讲清楚为什么不用。

**回答**

不能直接相加，因为向量检索的余弦相似度和BM25分数量纲不同，而且不同查询下分布也会变化。当前Orchestrator对两路结果按文档ID去重合并，再由默认的cross-encoder做统一相关性排序，不让不可比的原始分数直接参与最终决策。

仓库的 `MultiRetriever` 还实现了RRF：每路只使用排名，按 `1/(k+rank)` 累加，天然规避量纲问题。它适合在不希望增加专用重排服务时做轻量融合，也可以作为离线对照基线。但当前教师训练主流程为了直接比较题目与训练方向的相关性，默认走去重后cross-encoder精排。

这个回答的重点是区分“可复用能力”和“当前生产路径”：RRF代码是存在的，但不能声称当前Orchestrator已经调用它；如果后续统一线上与评估检索，可以把召回合并抽成共享策略并通过离线指标决定使用RRF还是直接Rerank。

***

### 10.仓库RRF实现里的k=60怎么理解？为什么不能说它是当前主链路的线上参数？

**分析**

面试官在追问实现细节，想看你对超参数的理解程度。k=60是RRF论文里的默认值，但你得知道它的作用和影响。

**回答**

k=60来自RRF常见默认设置，用来控制头部排名优势的平滑程度。k越小，第一名优势越明显；k越大，各名次差异越平缓。

当前代码把60作为通用RRF组件的默认值，但教师训练Orchestrator没有调用该组件，所以不能把它包装成已经在线调优的参数。合理说法是：它为后续RRF对照实验提供默认基线；若切入主链路，应使用教师题库评估集比较不同k值的Recall@K和MRR，而不是凭论文默认值宣称效果。

***

### 11.召回之后为什么还要Rerank？为什么默认用cross-encoder而不是通用LLM？

**分析**

Rerank是RAG pipeline里比较高级的步骤，面试官想看你对检索质量的理解——RRF融合之后的排序一定是最优的吗？LLM Rerank带来了什么额外价值？

**回答**

召回阶段追求“不漏”，但最终每个训练方向只选Top1原题，所以还需要精排判断“这道教师题是否真正对应当前能力方向”。例如“课堂评价”可能召回形成性评价、作业评价和教师绩效评价，关键词都相关，但只有前两类适合当前教学训练。

当前默认使用DashScope的 `gte-rerank-v2` cross-encoder，让查询和候选文档共同编码并返回相关性顺序。相比用通用LLM生成排序JSON，它延迟更稳定、输出协议更确定，也更适合高频检索。系统仍保留 `llm` 和 `none` 两种策略，可用于兼容和离线对照。

不做也能运行，`none`策略会保留合并后的原始顺序并截断TopK；但Top1更容易受召回顺序影响。重排属于增强环节而非主流程单点：cross-encoder请求失败、返回异常或缺少部分索引时，会保留未重排文档并降级，不阻断训练。

***

### 12.你的Rerank如何做策略切换和失败降级？

**分析**

这是一个工程健壮性的问题，面试官想看你对LLM输出不确定性的处理方式。很多人只考虑正常情况，面试官想看你的容错意识。

**回答**

我抽象了 `RerankStrategy` 接口，提供cross-encoder、LLM和none三种实现。默认cross-encoder返回候选文档索引；LLM策略返回排序后的文档ID数组；none策略用于无外部依赖的基线。

无论是网络超时、HTTP错误、JSON解析失败还是返回索引越界，都优先回退到原始候选顺序并按TopK截断，而不是让教师训练中断。因为重排是增强能力，真正不能丢的是用户已经开始的训练会话。

对部分有效的结果，代码先按模型返回顺序放入合法文档，再把模型遗漏的原文档补到末尾并去重，避免一次不完整响应造成候选集静默丢失。

***

### 13.你的BM25检索是纯内存实现的，分词是怎么做的？有什么局限？

**分析**

面试官在考察你对BM25实现细节的理解，以及是否意识到当前实现的局限性。一个成熟的工程师应该知道自己方案的短板。

**回答**

我的BM25是纯Go实现的内存倒排索引，没有依赖外部搜索引擎。分词方面目前用的是比较基础的方式——把常见中英文标点替换成空格，然后按空格分割。参数用的是BM25的标准值，k1=1.5控制词频饱和度，b=0.75控制文档长度归一化。

这个方式对带空格的英文或固定缩写还可以，但教师题库以中文为主，“依据学情设计分层提问”可能被当成一个长词，不能稳定拆出“学情”“分层”“提问”，这是当前BM25召回的明显限制。

当前通过向量检索进行语义互补，但不能因此掩盖中文BM25的短板。后续应接入jieba或更适合教育语料的分词方案，建立教师题库评估集后比较Recall@K和MRR，再决定是否替换实现。

***

### 14.你的Milvus向量检索用的是什么距离度量？Embedding模型选的什么？维度多少？

**分析**

面试官在考察向量数据库的具体使用细节，这些参数选择直接影响检索质量，答不上来说明可能不是自己做的。

**回答**

距离度量用的是COSINE，也就是余弦相似度。选余弦而不是L2（欧氏距离）的原因是，余弦相似度衡量的是方向一致性，不受向量长度影响，在文本语义匹配场景下表现更稳定。L2距离对向量的绝对值敏感，如果不同Embedding模型输出的向量模长不一致，L2的结果就不太可靠。

Embedding模型用通义千问 `text-embedding-v3`，当前维度1024。选择同一DashScope体系可以统一鉴权和运维，而且模型对中文教师题、课程标准和课堂情境文本有较好的基础语义表示；维度继续增大还会增加Milvus存储和检索成本，需要用评估数据证明收益。

在Milvus那边，索引类型用的是AUTOINDEX，让Milvus根据数据规模自动选择合适的索引结构，当前数据量不大的情况下这样最省心。

***

### 15.你的知识库写入Milvus的时候为什么要分批，每批10条？

**分析**

这看起来是个小细节，但面试官在考你对外部服务限制的工程经验。如果你踩过这个坑会很自然地答出来。

**回答**

这个是实际踩坑踩出来的。写入Milvus的时候需要先把文档文本通过Embedding模型转成向量，通义千问的Embedding API对单次请求的文本数量有限制，一次发太多会报错或者超时。所以我在Store方法里做了分批处理，每次最多10条文档，分批调Embedding再分批写入Milvus。

代码里用的是一个简单的for循环按batchSize=10切片，每批写入成功后累计ID，如果某一批失败就返回已写入的ID和错误信息，让调用方知道写到哪里了，方便做断点续传或者重试。这比一次性全量写入要稳健得多，特别是题库文件可能有几百道题的情况。

***

### 16.题库检索的时候是怎么做用户隔离的？为什么要隔离？

**分析**

用户隔离是一个多租户系统的基本能力，面试官想看你对数据安全和多用户场景的考虑。

**回答**

隔离的原因是不同学校、教研组或教师可能上传内部考核标准、校本课例和训练题，A用户的私有材料不能被B用户检索到。与此同时，系统还要提供一套公共教师题库作为冷启动资源，所以设计成“私有题库优先、公共题库无命中时兜底”。

在Milvus这一侧，我在数据表里加了一个user\_id字段，写入的时候每条文档都打上用户ID标记。检索的时候通过 `RetrieveByUser` 方法，在Milvus查询里加一个filter表达式 `user_id == "xxx"`，只检索属于该用户的文档。

BM25这一侧用了另一种方式——BM25Manager，按用户维护独立的BM25Retriever实例。每个用户有自己的倒排索引，用一个 `map[string]*BM25Retriever` 来管理，加了RWMutex做并发保护。这样检索的时候直接根据userID取对应的Retriever实例，天然隔离。

两侧隔离策略不同源于存储模型：Milvus集中存储，用filter隔离；BM25为内存索引，用实例隔离。Orchestrator先查当前userID，私有题库没有命中时再查 `default_user` 的内置教师题库，不会把其他真实用户的数据作为兜底。

***

## **三、RAG评估体系**

> 考察对RAG质量评估的理解，包括评估指标设计、离线评估流程、线上线下一致性等。

### 17.你的RAG有做质量评估吗？怎么评估的？

**分析**

RAG评估是很多项目的薄弱环节，做了评估的项目在面试中会有明显加分。面试官想看你是否有质量意识，以及评估的维度是否合理。

**回答**

做了离线检索质量评估。每条样本包含教师训练方向query和相关题目ID，评估系统复用“Milvus + BM25 → ID去重 → 可配置Rerank”的链路，计算Recall@10、Recall@20和MRR，并按教学能力topic和difficulty分组，输出最差Top10样本用于排查。

整套流程是命令行流水线：解析教师题库、向量化写入Milvus并导出manifest，再按教育理念、教学设计、课堂管理、学习评价等topic均匀采样生成数据集，最后输出Markdown和JSON报告。修改中文分词、TopK或Reranker后都可以做可复现对比，而不是凭演示感觉判断效果。

***

### 18.你的离线评估具体跑的什么指标？Recall@K和MRR分别衡量什么？

**分析**

面试官在考察你对信息检索评估指标的理解，这是基本功。能讲清楚每个指标衡量什么、怎么计算，说明你真的跑过评估。

**回答**

Recall@K衡量的是前K个检索结果里能覆盖多少标注的相关文档。计算公式是 `|Top-K ∩ Relevant| / |Relevant|`，分母是标注的相关文档总数。比如一个query标注了3个相关文档ID，检索返回了Top10的结果，其中命中了2个，那Recall@10就是2/3。我跑了Recall@10和Recall@20两个，前者看精准召回能力，后者看更宽松的覆盖能力。

MRR（Mean Reciprocal Rank）衡量的是第一个相关文档出现在第几位。如果第一个相关文档排在第3位，这条样本的RR就是1/3。所有样本的RR取平均就是MRR。MRR越高说明检索结果排序越好，用户不用翻很远就能看到想要的结果。

此外还记录命中文档ID和首次命中排名，便于定位“形成性评价为什么召回到课堂纪律题”这类单样本问题。按教学能力topic与difficulty分组后，可以判断问题来自某一类教育语料、题量不足还是检索参数。

***

### 19.你的评估流程和线上检索流程是怎么保持一致的？为什么要一致？

**分析**

这个问题考的是你对评估方法论的理解。如果评估用的检索流程和线上不一致，评估结果就没有参考价值，面试官想看你有没有意识到这个问题。

**回答**

评估模块的 `retrieveForEval` 必须和Orchestrator教师题库检索保持一致：Milvus按用户检索、BM25按用户检索、ID去重、再走同一种RerankStrategy。否则离线指标无法代表真实训练时的题库命中效果。

为什么要一致呢？因为评估的目的就是衡量线上检索的效果。如果评估时用了不同的检索逻辑，比如评估时没走Rerank但线上走了，那评估出来的指标就不反映真实的线上表现，可能评估指标很好但线上效果差，或者反过来。这样的评估就失去了指导优化的意义。

当前仍有一部分逻辑需要手动同步，这是明确的技术债。更稳妥的后续方案是抽取共享检索服务，同时支持“私有优先、内置题库兜底”，让训练主链路和离线评估调用同一份代码。

***

### 20.你怎么量化这个教师训练项目？RAG指标和业务训练指标有什么区别？

**分析**

这题专门防止“为了包装而编数字”。面试官想看你能否区分系统质量、训练过程和真实教育效果，以及哪些指标已经由代码产生、哪些必须等真实业务数据。

**回答**

我分三层量化。第一层是RAG质量，用Recall@K、MRR衡量教师训练方向能否召回正确题目；评估数据可由题库manifest按topic采样，再结合能力标签重叠生成可复现基线，之后逐步加入人工复核。

第二层是单轮训练过程指标，已经由后端根据会话事实确定性计算：训练完成率、平均作答得分、启发式追问触发率、教师题库命中率、三类任务覆盖率。这些不交给大模型生成，避免幻觉。第三层才是教育业务效果，例如前后测提升率、提升计划完成率、次周复测达标率和教研员认可度；这类指标必须基于真实试点数据，没有数据时只能讲埋点与实验设计，不能在简历里虚构提升百分比。

***

## **四、Agent记忆系统**

> 考察对记忆系统的设计理解，包括短期/长期记忆的划分、持久化方案、薄弱点追踪等。

### 21.系统如何记住教师学员之前的训练表现？

**分析**

记忆系统是Agent应用的核心能力之一，面试官想看你的设计是否有层次感，而不是简单地"把聊天记录存起来"。

**回答**

系统把记忆分成单轮上下文和跨轮教学能力记忆，因为两类信息的生命周期和用途不同。

单轮上下文的事实来源是 `InterviewState.QAHistory`，记录训练题、学员回答、得分和反馈。AI教研员每次构造Prompt时只取最近3轮原始问答，并结合压缩后的动态教学能力画像。这样既保留近期细节，也控制Token增长。

跨会话记忆记录教育理念、教学设计、课堂管理、评价与沟通等能力的历史得分、待训练项和训练摘要。Redis存高频更新的画像与弱项，MySQL保存完整训练记录、评估报告和提升计划。低于60分的能力点进入待训练列表，后续复测达到80分可移除，30天未再次出现则过期，避免永久贴标签。

这样下一轮可以结合当前考核标准筛选历史待训练能力，做到有目标的复测，而不是每次随机出题。

***

### 22.代码里为什么既有20条的ShortTermMemory，又在AI教研员里只取最近3轮？主链路到底用哪个？

**分析**

这是代码核查题，必须诚实区分“已实现组件”和“主流程实际接线”，不能看到一个类就说线上已经使用。

**回答**

当前主训练链路实际使用 `InterviewState.QAHistory`，AI教研员显式截取最近3轮；`ShortTermMemory(20)` 虽然在Orchestrator中初始化，也实现了按sessionID的滑动窗口，但当前没有被训练节点调用。因此不能把20条说成已经生效的线上上下文策略。

20条代表组件设计时为通用聊天保留的上限，3轮则是教师训练提问的Prompt窗口，两者原本可以分别承担存储窗口和模型消费窗口。但现在没有接线，属于可以删除的冗余或待完成的集成点。

后续有两个选择：一是删除未使用的ShortTermMemory，只保留QAHistory加画像；二是让主链路统一写入ShortTermMemory，再由不同Agent按需读取最近N条。要先明确唯一事实来源，避免两套上下文漂移。面试时把这个作为真实技术债讲，反而比硬包装更可信。

***

### 23.薄弱点追踪是怎么实现的？什么时候记录薄弱点，什么时候移除？

**分析**

薄弱点追踪是这个项目记忆系统的一个亮点设计，面试官想看你的具体实现逻辑，尤其是记录和移除的条件。

**回答**

每道训练题评分后，编排器遍历该题的教学能力标签，例如学情分析、课堂提问、形成性评价，逐个调用 `UpdateWeakPoints`。

能力点已经存在时更新最近得分和训练次数；低于60增加未达标次数。复测达到80分时从待训练列表移除。这里表达的是“当前作答证据达到训练目标”，不是给教师做永久能力定性。

如果这个技能点不在列表里，只有得分低于60的时候才新增为薄弱点。也就是说，60到80之间的分数不会新增薄弱点，但也不会移除已有的薄弱点——必须达到80分才算真正掌握。

60分记录、80分移除形成迟滞区间，避免一次边缘高分就清除待训练项。不过阈值是当前产品规则，后续应结合不同题型难度和教研员标注校准，不能说成教育测量上的绝对标准。

***

### 24.薄弱点有过期机制吗？为什么要设过期？

**分析**

面试官在深挖记忆系统的细节，看你是否考虑了时间维度的数据老化问题。

**回答**

有的，薄弱点有30天的过期时间。在 `GetWeakPoints` 方法里，取出薄弱点列表后会先过滤掉 `LastSeen` 超过30天的记录，只返回还在有效期内的薄弱点。

设置过期是因为教学能力会随着备课、试讲和教研发生变化，一个月前证据不足不代表现在仍然薄弱。30天未再次训练就淘汰旧记录，避免给学员贴长期“弱标签”；真正的成长趋势应保留在历史报告中，而不是永远影响下一轮出题。

过滤后按分数升序取Top10，控制传给训练规划Agent的上下文规模。它们还要再与当前教师岗位或考核标准做相关性过滤，因此不会因为历史记录多就偏离本轮训练目标。

***

### 25.长期记忆的持久化方案是什么？为什么用Redis+MySQL双存储？

**分析**

存储选型题，面试官想看你对不同存储的特点和适用场景是否理解，以及为什么用两个而不是一个。

**回答**

Redis存UserProfile、待训练能力和训练历史摘要等热数据，还存题库文件SHA256用于上传去重。Key中的 `interview:` 前缀属于历史兼容命名，不代表当前产品仍是通用求职面试；若做正式迁移，可以通过双写和版本化逐步改名，而不是直接破坏旧数据。

MySQL存完整训练记录、教学能力报告和提升计划JSON，这些数据写多改少，适合按用户和时间查询成长轨迹；MySQL同时承担认证用户表。

不只用一个存储，是因为画像属于高频读写小对象，完整训练记录属于低频写入、长期查询的大对象。Redis负责热状态，MySQL负责持久化，通过Store接口解耦；这不是为了堆技术，而是匹配两种访问模式。

***

### 26.出题的时候薄弱点是怎么参与进来的？会不会一直考同一个薄弱点？

**分析**

面试官在考察薄弱点和出题策略之间的联动机制，以及是否考虑了用户体验——一直被考同一个弱项的话体验很差。

**回答**

编排器会取出历史待训练能力，再与当前教师岗位或考核标准中的必备能力、进阶能力和核心方向做包含关系过滤。例如历史弱项是“形成性评价”，当前标准包含“学习评价”，就可以进入本轮上下文；若本轮目标是家校沟通，无关的学科教学弱项不会被强行带入。

过滤后的待训练能力作为额外上下文进入Phase1，但训练规划仍按教育理论、教学实践、课堂情境三类生成候选池。历史弱项只是个性化依据之一，不能覆盖目标标准和教学档案，更不能让整轮训练只重复一个问题。

重复控制依赖三个机制：当前目标过滤、题型配额保证覆盖面、复测达标后移除待训练项。30天过期还能防止陈旧信息长期干扰。后续可以再增加同能力点冷却轮次，进一步控制训练疲劳。

***

## **五、动态难度调节与出题策略**

> 考察出题策略的设计和实现细节，包括两阶段出题、难度动态调整、追问策略等。

### 27.你的出题流程分了两个阶段，为什么要分开而不是一步到位？

**分析**

两阶段出题是这个项目出题策略的核心设计，面试官想看你为什么不直接让LLM一步出完所有题目。

**回答**

Phase1先规划训练方向，包括教学能力点、题型、难度、教学档案上下文和检索query；Phase2再根据方向组装具体任务。这样先控制“训练覆盖什么”，再决定“用题库还是动态生成”，避免模型一步生成时遗漏某一类能力。

教师题库中有经过教研整理的教育理论题和参考作答要点，质量边界比临时生成更稳定，但题库可能很大，不能全部塞进Prompt。先生成检索方向，可以按能力点精准召回，同时让未命中的方向安全降级到LLM。

当前只让 `theory` 教育理论题优先走RAG；`practice` 教学实践题必须依据学员真实授课、实习、说课或教研经历，`scenario` 课堂情境题需要结合学段学科动态设定，所以交给LLM生成更合理。旧 `basic/experience/design` 仅通过兼容函数映射，不再作为对外业务题型。

***

### 28.动态难度调节的算法是怎么设计的？

**分析**

面试官想看你的难度调节是否有量化的规则，而不是拍脑袋决定。一个好的答案应该讲清楚触发条件和状态转换。

**回答**

难度分Easy、Medium、Hard三档，每个题型阶段进入时都从Medium开始。连续两题达到较好表现就上调一档，连续两题未达到阈值就下调一档；教育理论、教学实践和课堂情境之间会重置连续状态，避免某阶段的表现机械影响下一种能力。

当前代码用70分作为调度阈值：得分不低于70增加 `ConsecutiveRight`，否则增加 `ConsecutiveWrong`，再由 `AdjustDifficulty` 更新下一题目标难度。这里的“答对/答错”只是调度术语；对开放性的教学任务，评分还会考虑方案完整性、教学理由、学情适配和可实施性，参考答案不是唯一正确表述。

使用连续表现而不是累计正确率，是为了更快响应学员当前状态；阶段间重置则避免把教育理论优势误认为课堂应变也同样成熟。当前算法仍是启发式规则，不能包装成教育测量模型，后续可结合题目难度、教研员评分和复测数据校准。

要说局限的话，当前这个算法比较简单，没有考虑题目本身的难度分布——一道Hard题答对和一道Easy题答对的权重是一样的。后续可以加权处理，让Hard题答对更快升级，Easy题答错更快降级。

***

### 29.AI教研员什么时候会追问？为什么说它是训练而不只是考试？

**分析**

追问体现项目立意。面试官想看你是否只是用LLM多问一句，还是把追问设计成能暴露和改善教学思考的形成性训练。

**回答**

追问需要三个条件同时满足：评分结果建议追问；得分在30到80之间；`KeyPointsMissed` 非空。也就是只在学员已经有部分教学思路、但关键环节缺失的“可支架区间”追问。过低时本轮先给基础反馈，过高时避免无效打断。

追问优先指向学情依据、教学目标、活动设计或评价闭环中最关键的缺口，例如“你会用什么课堂证据判断这个活动真的达成目标”。这样系统不直接代答，而是引导学员补全教学决策链。

AI教研员只拿实际回答、评分反馈和待补充能力生成一句苏格拉底式追问，Prompt明确禁止捏造学员没有说过的经历。当前只追问一轮，用于控制会话长度；追问是否发生还会进入“启发式追问触发率”这一确定性训练指标。

***

### 30.题库匹配到的原题和LLM生成的题目，在使用上有什么区别？

**分析**

面试官在考察你对两种题目来源的处理差异，一个好的答案应该讲清楚为什么要区别对待。

**回答**

区别主要体现在三个地方。第一个是构建方式：题库原题在编排器里直接用代码构建 `PlannedQuestion` 结构体，不经过Phase2的LLM调用。它的content取的是题库文档里的题目原文，reference取的是参考答案部分，source字段记录的是Milvus里的文档ID。而LLM生成的题目是经过Phase2 Prompt让LLM产出的。

第二个是训练过程中的可追溯标注：题库原题显示文档ID，动态生成题显示LLM来源，便于报告复盘、题库命中率统计和问题排查。

第三个是低分巩固：题库题优先展示教研整理的参考要点，缺失时才尝试再次检索；LLM题使用生成时附带的参考作答要点。对开放教学问题，reference被定位为观察维度和合理依据，不宣称唯一标准答案。

***

### 31.出题方向规划的时候，对题目数量有严格要求吗？为什么？

**分析**

面试官想看你对Prompt工程的实践经验——LLM输出不可控，你怎么保证出题数量符合预期。

**回答**

有，而且区分“候选题池”和“实际训练量”。Phase1要求 `theory` 每个难度5个共15个、`practice` 每个难度4个共12个、`scenario` 中难度和高难度各2个共4个。候选池铺满是为了让动态难度调节有题可选，并不意味着一轮要问31题。

实际调度上限是教育理论8题、教学实践5题、课堂情境2题，共15题。调度器按当前难度从候选池取题，目标难度为空时再就近回退。这样既控制单轮时长，又保证不同表现的学员有足够候选题。

三类配额服务不同目的：教育理论保证课程标准和教育理念基础面；教学实践需要围绕真实档案做较深复盘；课堂情境耗时更长，用较少题验证综合应变。若教学档案没有真实经历，Prompt要求改为通用教学决策题，不能杜撰经历来凑配额。

Phase2的Prompt里也有类似的强约束："每个出题方向必须对应生成恰好一道题目，不得合并、删减或跳过任何方向"。这样从方向到题目是严格的一对一，数量就可控了。

***

## **六、Skill扩展机制与业务边界**

> 当前代码保留Skill框架和历史技术Skill源码，但Web启动不再注册它们。回答重点是为什么主动收缩能力边界，以及教师Skill未来如何接入，不要把停用能力说成线上功能。

### 32.Tool、Agent、Skill有什么区别？为什么教师训练版本暂时不注册旧技术Skill？

**分析**

面试官既看架构抽象，也看产品克制。如果为了展示Skill而继续暴露Redis测验、技术对比等入口，就会与教育立意冲突。

**回答**

Tool是无状态、单次输入输出明确的外部能力，例如抓取目标考核标准网页或搜索数字化教学开源工具。Agent承担一个有明确职责的推理节点，例如标准解析、训练规划和形成性评价。Skill介于两者之间，适合有状态但不需要走完整DAG的多轮任务，例如未来的“说课结构拆解”或“课堂事件复盘”。

旧版本已有快速技术测验、技术概念讲解、项目亮点和技术对比Skill，但它们服务的是通用程序员面试，与当前教师能力训练立意不一致。因此我保留Skill接口、Registry和状态机代码用于扩展，却在Web启动时不注册这些历史Skill。这样不是删技术，而是把“可复用机制”和“当前产品能力”分开。

主训练闭环仍由Graph Agent负责，因为它必须先有目标标准和教学档案；只有目标单一、状态较轻的教师教研任务才适合做Skill。这个边界能防止为了堆架构而强行加入功能。

***

### 33.如果后续增加教师Skill，意图匹配和优先级怎么做？

**分析**

面试官想看你对Skill激活机制的实现细节，以及多个Skill可能冲突时的处理方式。

**回答**

每个Skill实现 `Match(input string) bool`，Registry按注册顺序返回第一个命中项，先注册优先级更高。未来可以让“帮我拆一份说课稿”命中说课拆解Skill，“复盘这次课堂冲突”命中课堂事件复盘Skill，而“开始训练”仍进入完整DAG。

规则匹配适合意图数量少、表达边界清晰的第一版；关键词应围绕任务动作和教育对象联合判断，不能只看到“练习”就误触发。冲突增多后可以增加显式优先级、置信度或小模型分类，但在没有数据证明必要之前不引入额外LLM调用。

当前教师版本的SkillRegistry为空，所有普通问答由AI教研助手处理，这是有意的产品边界，而不是初始化遗漏。

***

### 34.如果实现“教师资格快速练习”Skill，题目不够时应该怎样降级？

**分析**

面试官想看你对RAG和LLM的降级策略——RAG检索不到足够的题目时怎么兜底。

**回答**

这题是扩展设计，不要回答成已经上线。合理策略是三层来源：先查用户私有教师题库，再查系统内置教师题库，仍不足时才让LLM按教师资格大纲和指定学科学段补题。每道题必须保留来源，便于教研审核。

LLM补题要约束 `theory/practice/scenario` 类型、难度、能力标签和参考作答要点；实践题不能虚构用户经历，情境题必须明确是“假设课堂”。题库完全为空时可以完成体验，但界面应提示题目为AI生成，不能冒充权威考试原题。

如果私有题库和系统题库都没有命中，应记录降级原因；若LLM也失败，则返回可恢复错误，不让一个轻量Skill影响主训练会话。

这个方案复用了现有“私有优先、内置兜底、LLM补齐”的思路，但是否实现要由真实教师高频需求决定。

***

### 35.SkillState的会话过期时间设了30分钟，这个设计考虑了什么？

**分析**

面试官想看你对有状态会话的超时管理的理解。

**回答**

30分钟过期用于避免有状态教研任务“僵尸化”。例如教师开始复盘一节课，输入到一半离开；长时间后再回来，继续推送旧步骤既占内存也容易丢失语境。

30分钟是工程经验值，不是教育规则。`IsExpired` 返回true后，WebSocket会话层清理SkillState，下一次输入重新做意图匹配；若未来Skill承载长周期备课，则应把草稿持久化，而不是简单延长内存超时。

另外用户也可以主动退出，输入"/quit"、"/exit"或者"退出"，会被 `IsQuitCommand` 捕获，立即结束当前Skill。这两种机制——超时自动清理和主动退出——保证了Skill状态不会无限累积。

***

## **七、MCP协议与工程化**

> 考察MCP集成、WebSocket通信、题库去重等工程化能力。

### 36.你的MCP集成具体做了什么？为什么用MCP而不是直接调API？

**分析**

MCP是大模型工具协议的新标准，面试官想看你对MCP的理解和实际使用经验。

**回答**

我接了两类MCP能力。第一类是Playwright MCP，通过无头浏览器读取JS渲染页面的accessibility snapshot，再由LLM提取教师岗位要求、教师资格面试大纲或教学能力考核标准。第二类是GitHub MCP，只有当提升计划明确涉及数字化教学工具、学习分析或教育技术实践时，ReviewPlanner才通过ReAct按需调用 `search_repositories`；普通教学设计弱项不会为了展示工具而推荐开源项目。

使用MCP的核心价值是统一工具发现、参数协议和stdio生命周期，网页抓取与GitHub搜索可以沿用同一种客户端接入方式。GitHubSearcher本身通过mcp-go启动官方Server，ReviewPlanner再用Eino `InferTool` 暴露为ReAct可调用工具，实现“外部工具接入”和“Agent自主决定是否调用”两层解耦。

MCP的代价是启动npx子进程、初始化有30秒超时、stdio调用需要串行保护，目标网站还可能反爬。因此两类工具都是可降级能力：网页失败时允许粘贴文本，GitHub失败时只生成不带虚构链接的教学提升计划。

***

### 37.WebSocket会话管理是怎么做的？一个连接上怎么区分不同的消息类型？

**分析**

面试官想看你对WebSocket在实际项目中的使用经验，包括消息协议设计和状态管理。

**回答**

每个WebSocket连接对应一个WSSession实例，里面维护了当前用户ID、聊天历史、答案channel、当前激活的Skill及其状态等会话级数据。

消息通过JSON的 `type` 字段区分。新协议使用 `start_training`，并用 `assessment` 和 `profile` 传目标标准与教学档案；回答、教师题库上传和退出分别使用 `answer`、`upload_questions`、`quit_training`。后端仍兼容旧 `start_interview/jd/resume/quit_interview`，避免已有客户端一次性失效。服务端通过stage、question、score、report和review_plan推送训练进度与结果。

主循环持续读消息并switch分发。训练DAG在独立goroutine中执行，需要学员回答时通过 `GetUserAnswer` 阻塞在channel；WebSocket收到answer后写入 `answerCh`，编排器继续。generation、completionSent和currentQuestionID共同避免旧会话消息、重复回答或终止后的迟到消息污染新训练。

WSSession仍保留activeSkill和skillState扩展位，但教师版本启动时Registry为空，因此不会误触发历史技术Skill；普通chat进入AI教研助手，完整训练必须通过标准化入口启动。

***

### 38.题库上传的时候怎么做去重的？支持哪些上传场景？

**分析**

题库去重是一个工程细节，面试官想看你对数据一致性问题的处理方式。

**回答**

去重的核心机制是SHA256文件级哈希。用户上传题库文件的时候，先对文件内容算SHA256哈希值，然后和Redis里存的历史哈希做比对（Key是 `interview:file_hash:{userID}:{filename}`）。

支持三种场景：第一种是"文件内容完全没变"——哈希一致，直接跳过，不做任何写入操作，告诉用户"文件内容未变化，跳过"。第二种是"同名文件但内容变了"——哈希不同，走覆盖逻辑，先调Milvus的 `DeleteBySourceFile` 删掉该用户该文件的旧题目，再写入新题目，同时BM25Manager也重建索引。第三种是"全新文件"——Redis里没有这个文件的哈希记录，直接追加写入。

写入前由QuestionParser把教师资格题、试讲答辩题或课堂情境材料解析为结构化题目，类型只能是 `theory/practice/scenario`，并校验题干、参考作答要点、难度和教学能力标签。历史 `basic/project/design/algorithm` 会在兼容层归一化，不再作为新题库对外口径。校验成功后写入Milvus和BM25，最后保存文件哈希。

***

## **八、拓展优化题**

> 考察架构扩展能力和技术深度，这些题目是在项目核心问题都问完之后的延伸思考。

### 39.如果要支持100名教师同时训练，你会怎么改架构？

**分析**

这是一道典型的系统扩展题，面试官想看你的架构思维和对性能瓶颈的判断能力。不是让你设计一个完美方案，而是看你能否准确识别瓶颈并给出合理的优化方向。

**回答**

最大瓶颈是模型调用：每轮包含标准解析、档案诊断、规划、逐题评分、追问、报告与提升计划。100名教师并发会产生大量突发请求，需要按用户和模型做限流、队列、超时与重试，并把可以并行的准备阶段受控并发；是否使用自部署模型要通过教育语料质量、成本和运维能力评估，不能只看吞吐。

第二是BM25按用户保存在单机内存，用户和校本题库增加后内存线性增长。可迁移到Elasticsearch/OpenSearch，并用tenant/user过滤实现私有数据隔离，同时保留公共教师题库命名空间。

第三是会话状态与WebSocket路由。每轮训练在goroutine中运行，Go能承担连接量，但服务水平扩展后需要把会话检查点外置到Redis/MySQL，并通过粘性路由或消息队列保证answer回到正确训练实例；还要给学校或组织设置租户级配额，避免单一租户占满模型额度。

WebSocket这层影响不大，gorilla/websocket本身能撑住几千个并发连接，只要服务器内存够。

***

### 40.在做这个项目过程中，你遇到了哪些困难，是怎么解决的？

**分析**

这是面试中最经典的开放题之一，考察你的工程实践能力和问题解决思路。不要泛泛而谈，要挑具体的、有技术深度的困难，说清楚问题现象、排查过程和最终方案。

**回答**

第一个困难是**教师题库召回既要理解语义，又要命中教育术语**。只用向量检索时，“形成性评价”可能召回泛化的课堂反馈题；只用当前简易BM25时，中文长短语又容易漏召回。我引入Milvus和BM25双路召回、ID去重以及可切换Reranker，默认使用cross-encoder做Top1精排，并搭建Recall@K/MRR离线评估。这里要诚实说明：当前中文BM25仍是基础分词，RRF组件也没有接入Orchestrator主链路，后续需要用教师语料评估后再优化。

第二个困难是**大文件教师题库解析的Token溢出**。我按题目标题或段落边界把文件拆成不超过约25000字符的segment，逐段调用LLM并合并结果；若JSON被截断，再尝试恢复最后一个完整对象。切分不能把一段课堂情境和参考要点拆开，否则会生成无依据的问题。

第三个困难是**私有校本题库与公共教师题库的边界**。Milvus通过user_id过滤，BM25Manager按用户维护独立索引；Orchestrator先检索当前用户，只有没有命中时才回退到 `default_user` 的内置教师题库，绝不会查询其他用户。这样同时解决多租户隐私和新用户冷启动，但BM25内存仍随用户数增长，是规模化时需要外置的技术债。
